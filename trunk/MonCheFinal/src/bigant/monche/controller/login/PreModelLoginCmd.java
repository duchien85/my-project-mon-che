package bigant.monche.controller.login;

import org.puremvc.java.interfaces.INotification;
import org.puremvc.java.patterns.command.SimpleCommand;

import bigant.monche.names.ProxyNames;

public class PreModelLoginCmd extends SimpleCommand {
	public void execute(INotification note) {
		//LoginServiceProxy networkLoginProxy = new LoginServiceProxy( ProxyNames.LOGIN_PROXY);
		
     	//facade.registerProxy( networkLoginProxy );
     }
}
