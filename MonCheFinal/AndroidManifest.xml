<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.monche.app"
    android:versionCode="3"
    android:versionName="2.0" >

    <uses-sdk android:minSdkVersion="7" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name" 
            android:name="bigant.monche.view.activity.SplashScreen">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/app_name"
            android:name="bigant.monche.view.activity.MonCheActivity"> 
           
        </activity>
        <activity
            android:label="@string/app_name"
            android:name="bigant.monche.view.activity.ImageGalleryActivity" />
        <activity
            android:label="@string/app_name"
            android:name="bigant.monche.view.activity.ImageGridActivity" />
        <activity
            android:label="@string/app_name"
            android:name="bigant.monche.view.activity.LoginActivity" />
        <activity
            android:label="@string/app_name"
            android:name="bigant.monche.view.activity.SignInActivity" />
        <activity
            android:label="@string/app_name"
            android:name="bigant.monche.view.activity.MonCheActivity" />
        <activity android:name="com.google.ads.AdActivity"
              android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"/>
    </application>

</manifest>